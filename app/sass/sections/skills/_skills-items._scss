@include b('skills'){
	&__items{
		position: relative;
		display: flex;
		flex-flow: row wrap;
		margin-left: -15px;
		margin-right: -15px;
		list-style: none;
		&-wrapper{
			position: relative;
			max-width: 80%;
			margin-left: auto;
			margin-right: auto;
			@include min(sm){
				height: 460px;
			}
		}
	}
}